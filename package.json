{
  "name": "task-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "install:all": "yarn --cwd client install && yarn --cwd server install",
    "type-check:server": "tsc --noEmit -p server/tsconfig.json",
    "type-check:client": "tsc --noEmit -p client/tsconfig.json"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.46.1",
    "@typescript-eslint/parser": "^8.46.1",
    "eslint": "^9.37.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "husky": "^9.0.0",
    "lint-staged": "^16.2.4",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "server/**/*.ts": [
      "eslint --fix --max-warnings=0 --config server/eslint.config.mjs",
      "prettier --write"
    ],
    "client/**/*.{ts,tsx}": [
      "yarn --cwd client lint --fix --max-warnings=0",
      "prettier --write"
    ],
    "*.{json,md,yml}": [
      "prettier --write"
    ]
  }
}
