// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../../generated/prisma"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"] 
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Apartment {
  id          Int      @id @default(autoincrement())
  unit_name   String   @db.VarChar(255)
  unit_number String   @db.VarChar(255) @unique
  project     String   @db.VarChar(255)
  address     String
  price       Decimal  @db.Decimal(10, 2)
  bedrooms    Int
  description String   @db.VarChar(255)
  createdAt   DateTime @default(now())
  status      String   @default("available") @db.VarChar(20)
  amenities   String[] @default([])
  images      String[] @default([])
  area        Decimal  @default(0) @db.Decimal(8, 2)
  bathrooms   Int      @default(1)

  @@index([project], map: "idx_project")
  @@index([unit_number], map: "idx_unit_number")
  @@index([status], map: "idx_status")
}
